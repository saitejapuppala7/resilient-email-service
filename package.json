{
  "name": "resilient-email-service",
  "version": "1.0.0",
  "description": "A resilient email sending service with retry, fallback, rate-limiting, and circuit breaker",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/server.ts",
    "test": "jest"
  },
  "keywords": ["email", "resilient", "typescript", "retry", "circuit-breaker"],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.12",
    "jest": "^30.0.4",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ts-node": "^10.9.2"
  }
}
